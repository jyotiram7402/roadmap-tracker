// data/maven/notes.js — Maven study notes (topic-wise).
// Shape: TOPICS = [ { title, slug, points: [ block ] } ]
// block kinds: {kind:"text",text} | {kind:"subhead",text} | {kind:"note",text}
//              | {kind:"code",lines:[]} | {kind:"mermaid",code:"..."}
// Populated from the user's Maven notes once provided as text/images.
export const TOPICS = [];
