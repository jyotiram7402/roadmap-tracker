// data/springboot-qa/web.js — Spring Boot web/data/security Q&A.
// Original explanations written from an interview point of view (Spring Boot 3, jakarta.*).
export const QUESTIONS = [
  {
    q: "Explain the concept of a starter dependency.",
    slug: "starter-dependency",
    answer: [
      { kind: "text", text: "A starter is a curated, opinionated set of dependencies bundled under a single artifact so you don't have to hunt for individual libraries and matching versions. Adding one starter pulls in everything needed for a capability, with versions already aligned by the Spring Boot BOM (bill of materials)." },
      { kind: "text", text: "Interview angle: spring-boot-starter-web brings in Spring MVC, Jackson, validation and an embedded Tomcat. You add one line and get a working web stack. Combined with auto-configuration, the starter also triggers sensible default beans." },
      { kind: "code", lines: ["<dependency>", "    <groupId>org.springframework.boot</groupId>", "    <artifactId>spring-boot-starter-web</artifactId>", "    <!-- no version: managed by the spring-boot parent/BOM -->", "</dependency>"] },
      { kind: "note", text: "You can exclude transitive pieces (e.g. exclude spring-boot-starter-tomcat and add spring-boot-starter-jetty to swap the server). Starters are conventions, not magic — nothing stops you from overriding them." },
    ],
    learn: "spring boot starter dependency explained",
  },
  {
    q: "How does Spring Boot support handling of JSON data (Jackson)?",
    slug: "json-handling-jackson",
    answer: [
      { kind: "text", text: "Spring Boot auto-configures Jackson as the default JSON library via spring-boot-starter-json (pulled in by the web starter). It registers a MappingJackson2HttpMessageConverter, so @RestController methods automatically serialize returned objects to JSON and deserialize request bodies into Java objects — no manual parsing." },
      { kind: "text", text: "Interview angle: the ObjectMapper is a Spring bean you can inject or tune. You customize it through application.properties (e.g. spring.jackson.*) or by contributing a Jackson2ObjectMapperBuilderCustomizer bean, and control field mapping with Jackson annotations." },
      { kind: "code", lines: ["@RestController", "public class UserController {", "    @PostMapping(\"/users\")", "    public User create(@RequestBody User user) { // JSON -> User", "        return user;                              // User -> JSON", "    }", "}", "", "public class User {", "    @JsonProperty(\"full_name\") private String name;", "    @JsonIgnore private String password;   // never serialized", "    @JsonFormat(pattern = \"yyyy-MM-dd\") private LocalDate dob;", "}"] },
      { kind: "note", text: "Common gotcha: LocalDate/LocalDateTime need jackson-datatype-jsr310 (auto-included) and spring.jackson.serialization.write-dates-as-timestamps=false for ISO strings instead of epoch numbers." },
    ],
    learn: "spring boot jackson json serialization configuration",
  },
  {
    q: "What is Spring Data JPA, and how does it integrate with Spring Boot?",
    slug: "spring-data-jpa",
    answer: [
      { kind: "text", text: "Spring Data JPA is an abstraction over JPA/Hibernate that removes boilerplate DAO code. You declare a repository interface extending JpaRepository and Spring generates the implementation at runtime, giving you CRUD, paging and sorting for free, plus derived query methods parsed from method names." },
      { kind: "text", text: "Interview angle: spring-boot-starter-data-jpa auto-configures the DataSource, EntityManagerFactory and a JpaTransactionManager from your properties. @SpringBootApplication triggers repository scanning, so you just inject the interface — no XML, no manual EntityManager wiring." },
      { kind: "code", lines: ["@Entity", "public class Product {", "    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)", "    private Long id;", "    private String name;", "    private String category;", "}", "", "public interface ProductRepository extends JpaRepository<Product, Long> {", "    List<Product> findByCategory(String category);   // derived query", "    @Query(\"select p from Product p where p.name like %:kw%\")", "    List<Product> search(String kw);", "}"] },
      { kind: "note", text: "Uses jakarta.persistence.* (jakarta.persistence.Entity) in Spring Boot 3, not the old javax.persistence.* — a frequent migration gotcha." },
    ],
    learn: "spring data jpa repository spring boot integration",
  },
  {
    q: "What is the purpose of @RepositoryRestResource?",
    slug: "repository-rest-resource",
    answer: [
      { kind: "text", text: "@RepositoryRestResource, from Spring Data REST, exposes a Spring Data repository directly as a hypermedia (HATEOAS) REST API without writing any controller. Add spring-boot-starter-data-rest and your repositories are auto-published as /products endpoints supporting GET/POST/PUT/DELETE and paging." },
      { kind: "text", text: "Interview angle: it's for rapid CRUD APIs and admin backends. The annotation lets you customize the exposed path and relation name, or hide a repository from export entirely." },
      { kind: "code", lines: ["@RepositoryRestResource(path = \"products\", collectionResourceRel = \"products\")", "public interface ProductRepository extends JpaRepository<Product, Long> {", "    // auto-exposed: GET/POST /products, GET/PUT/DELETE /products/{id}", "}", "", "// hide a repository from the REST export:", "@RepositoryRestResource(exported = false)", "public interface InternalRepo extends JpaRepository<Secret, Long> {}"] },
      { kind: "note", text: "Gotcha: it bypasses your service layer, so validation/business rules must live in entities or event handlers. Many teams prefer explicit @RestControllers for public APIs to keep control over the contract." },
    ],
    learn: "spring data rest RepositoryRestResource annotation",
  },
  {
    q: "Explain the difference between @Component, @Service, and @Repository.",
    slug: "component-service-repository",
    answer: [
      { kind: "text", text: "All three are stereotype annotations that mark a class as a Spring-managed bean picked up by component scanning — technically @Service and @Repository are themselves meta-annotated with @Component. The difference is semantic intent and, for @Repository, extra behaviour." },
      { kind: "text", text: "Interview angle: @Component is the generic stereotype; @Service marks the business/service layer (no added behaviour, just clarity); @Repository marks the persistence layer AND enables exception translation — it converts vendor-specific DB exceptions into Spring's DataAccessException hierarchy." },
      { kind: "code", lines: ["@Repository", "public class JdbcProductDao {              // + persistence exception translation", "    // SQLException -> DataAccessException", "}", "", "@Service", "public class ProductService {              // business logic layer", "    private final JdbcProductDao dao;", "    public ProductService(JdbcProductDao dao) { this.dao = dao; }", "}", "", "@Component", "public class NotificationHelper {}         // generic utility bean"] },
      { kind: "note", text: "Functionally you could use @Component everywhere and it would still wire up — but the specific stereotypes document layering and @Repository's exception translation is a real, not just cosmetic, benefit." },
    ],
    learn: "difference between Component Service Repository spring",
  },
  {
    q: "What is the purpose of the @Bean annotation?",
    slug: "bean-annotation",
    answer: [
      { kind: "text", text: "@Bean is a method-level annotation used inside a @Configuration class to explicitly declare a bean. The method's return value is registered in the application context, and the method name becomes the bean id. It's the programmatic alternative to @Component when you can't annotate the class — typically third-party types." },
      { kind: "text", text: "Interview angle: use @Component for your own classes (auto-detected by scanning) and @Bean for objects you construct manually or from libraries you don't own, or when creation needs custom logic." },
      { kind: "code", lines: ["@Configuration", "public class AppConfig {", "    @Bean", "    public RestTemplate restTemplate() {   // third-party type -> a bean", "        return new RestTemplateBuilder()", "            .setConnectTimeout(Duration.ofSeconds(5))", "            .build();", "    }", "}"] },
      { kind: "note", text: "By default @Bean methods produce singletons, and calling one @Bean method from another returns the same managed instance (CGLIB proxying of the @Configuration class ensures this)." },
    ],
    learn: "spring bean annotation vs component configuration",
  },
  {
    q: "Explain the concept of Bean scopes in Spring Boot.",
    slug: "bean-scopes",
    answer: [
      { kind: "text", text: "A bean scope defines the lifecycle and visibility of a bean instance in the container. The default is singleton — one shared instance per Spring context. prototype creates a new instance on every injection/lookup. Web-aware scopes (request, session, application, websocket) tie a bean's life to an HTTP request, session, etc." },
      { kind: "text", text: "Interview angle: singleton beans must be stateless/thread-safe because one instance is shared across all threads. Reach for prototype or request scope only when you genuinely need per-use or per-request state." },
      { kind: "code", lines: ["@Component", "@Scope(\"prototype\")                 // new instance each time", "public class ReportBuilder {}", "", "@Component", "@Scope(value = WebApplicationContext.SCOPE_REQUEST,", "       proxyMode = ScopedProxyMode.TARGET_CLASS)  // per HTTP request", "public class RequestContext {}"] },
      { kind: "note", text: "Gotcha: injecting a shorter-lived scope (prototype/request) into a singleton needs a scoped proxy or ObjectProvider — otherwise the singleton captures one instance at startup and the scope is effectively ignored." },
    ],
    learn: "spring bean scopes singleton prototype request session",
  },
  {
    q: "What is the use of @Primary?",
    slug: "primary-annotation",
    answer: [
      { kind: "text", text: "@Primary marks one bean as the default choice when multiple candidates of the same type exist. Without it, an ambiguous @Autowired by type throws NoUniqueBeanDefinitionException; @Primary resolves that ambiguity by giving one bean preference." },
      { kind: "text", text: "Interview angle: it's the coarse-grained way to break ties. For explicit, per-injection selection use @Qualifier instead — @Qualifier at the injection point wins over @Primary." },
      { kind: "code", lines: ["@Service", "@Primary", "public class EmailNotifier implements Notifier {}", "", "@Service", "public class SmsNotifier implements Notifier {}", "", "@Service", "public class AlertService {", "    // gets EmailNotifier (the @Primary) unless a @Qualifier says otherwise", "    public AlertService(Notifier notifier) {}", "}"] },
      { kind: "note", text: "Only one bean of a given type should be @Primary; marking two reintroduces the ambiguity it was meant to solve." },
    ],
    learn: "spring Primary annotation vs Qualifier",
  },
  {
    q: "What is the purpose of the @Value annotation in Spring Boot?",
    slug: "value-annotation",
    answer: [
      { kind: "text", text: "@Value injects externalized configuration — property values, environment variables, or SpEL expressions — into fields, constructor parameters or method arguments. It resolves ${...} placeholders from application.properties/YAML at bean creation time." },
      { kind: "text", text: "Interview angle: it's great for a handful of scattered values. For a whole group of related properties, prefer @ConfigurationProperties, which binds a typed POJO and supports validation and relaxed binding better than many @Value fields." },
      { kind: "code", lines: ["@Component", "public class MailConfig {", "    @Value(\"${app.mail.host}\") private String host;          // from properties", "    @Value(\"${app.mail.port:25}\") private int port;          // 25 = default", "    @Value(\"#{systemProperties['user.timezone']}\") String tz; // SpEL", "}"] },
      { kind: "note", text: "Gotcha: @Value on a field can't be used easily in the constructor (field injection happens after construction). Prefer constructor-parameter @Value if you need the value during construction." },
    ],
    learn: "spring boot Value annotation property injection",
  },
  {
    q: "What is the purpose of the @Validated annotation?",
    slug: "validated-annotation",
    answer: [
      { kind: "text", text: "@Validated (org.springframework.validation.annotation.Validated) is Spring's wrapper around Bean Validation that turns on validation for a bean. On a class it enables method-level parameter/return validation; it also adds support for validation GROUPS, which the plain jakarta @Valid does not." },
      { kind: "text", text: "Interview angle: put @Validated on a @Service or @RestController class to validate method arguments, or use it on a controller method param (often interchangeably with @Valid). Use groups when the same DTO has different rules for create vs update." },
      { kind: "code", lines: ["@Service", "@Validated                                   // enable method validation", "public class PricingService {", "    public BigDecimal quote(@Min(1) int qty,", "                            @NotBlank String sku) { ... }", "    // violations -> ConstraintViolationException", "}", "", "// with groups on a controller:", "public User update(@Validated(OnUpdate.class) @RequestBody User u) { ... }"] },
      { kind: "note", text: "Difference to remember: @Valid is standard jakarta.validation and cascades into nested objects; @Validated is Spring-specific and adds group support and class-level method validation." },
    ],
    learn: "spring Validated vs Valid annotation groups",
  },
  {
    q: "How can you enable server-side validation?",
    slug: "enable-server-side-validation",
    answer: [
      { kind: "text", text: "Add spring-boot-starter-validation (Hibernate Validator, the Bean Validation reference implementation), annotate DTO fields with jakarta.validation constraints, then trigger validation by placing @Valid (or @Validated) on the controller method's request-body parameter." },
      { kind: "text", text: "Interview angle: when @Valid is present, Spring runs the validator before the method body. If a bound request body fails, it throws MethodArgumentNotValidException, which Spring maps to a 400 Bad Request by default." },
      { kind: "code", lines: ["public class UserDto {", "    @NotBlank private String name;", "    @Email private String email;", "    @Min(18) private int age;", "}", "", "@RestController", "public class UserController {", "    @PostMapping(\"/users\")", "    public User create(@Valid @RequestBody UserDto dto) {  // validated first", "        return service.save(dto);", "    }", "}"] },
      { kind: "note", text: "Constraints live in jakarta.validation.constraints.* in Spring Boot 3 (not javax.*). Also: without spring-boot-starter-validation on the classpath, the annotations are silently ignored." },
    ],
    learn: "enable bean validation spring boot rest controller",
  },
  {
    q: "How can you handle validation errors?",
    slug: "handle-validation-errors",
    answer: [
      { kind: "text", text: "Two approaches. Capture errors inline by adding a BindingResult parameter immediately after the @Valid object, then inspect getFieldErrors(). Or let the exception propagate and handle MethodArgumentNotValidException centrally in a @RestControllerAdvice to return a structured error response." },
      { kind: "text", text: "Interview angle: the global @ExceptionHandler approach is preferred in real APIs — it keeps controllers clean and produces a consistent error payload across all endpoints." },
      { kind: "code", lines: ["@RestControllerAdvice", "public class ValidationAdvice {", "    @ExceptionHandler(MethodArgumentNotValidException.class)", "    @ResponseStatus(HttpStatus.BAD_REQUEST)", "    public Map<String,String> onInvalid(MethodArgumentNotValidException ex) {", "        Map<String,String> errors = new HashMap<>();", "        ex.getBindingResult().getFieldErrors().forEach(", "            e -> errors.put(e.getField(), e.getDefaultMessage()));", "        return errors;   // { \"email\": \"must be a valid email\" }", "    }", "}"] },
      { kind: "note", text: "For @Validated method-level violations (not request bodies) the exception is ConstraintViolationException instead — handle both if you use both styles." },
    ],
    learn: "handle validation errors MethodArgumentNotValidException spring boot",
  },
  {
    q: "How can you handle exceptions in a Spring Boot application?",
    slug: "handle-exceptions",
    answer: [
      { kind: "text", text: "Several layers. Locally with a try/catch or a per-controller @ExceptionHandler method; globally with a @ControllerAdvice / @RestControllerAdvice class holding @ExceptionHandler methods; and out of the box Spring Boot returns a default JSON error via BasicErrorController." },
      { kind: "text", text: "Interview angle: the recommended production pattern is a global @RestControllerAdvice that maps exception types to HTTP statuses and a consistent error DTO, so error handling is centralized and not scattered across controllers." },
      { kind: "code", lines: ["@RestControllerAdvice", "public class GlobalExceptionHandler {", "    @ExceptionHandler(ResourceNotFoundException.class)", "    @ResponseStatus(HttpStatus.NOT_FOUND)", "    public ApiError notFound(ResourceNotFoundException ex) {", "        return new ApiError(404, ex.getMessage());", "    }", "    @ExceptionHandler(Exception.class)          // catch-all fallback", "    @ResponseStatus(HttpStatus.INTERNAL_SERVER_ERROR)", "    public ApiError generic(Exception ex) {", "        return new ApiError(500, \"Unexpected error\");", "    }", "}"] },
      { kind: "note", text: "You can also extend ResponseEntityExceptionHandler to reuse Spring's handling of built-in MVC exceptions and only override what you need." },
    ],
    learn: "exception handling spring boot rest api best practices",
  },
  {
    q: "How does Spring Boot support global exception handling?",
    slug: "global-exception-handling",
    answer: [
      { kind: "text", text: "Global handling is provided by @ControllerAdvice (or @RestControllerAdvice), a class whose @ExceptionHandler methods apply across ALL controllers rather than one. Spring intercepts exceptions thrown from any handler method and routes them to the matching advice method." },
      { kind: "text", text: "Interview angle: this centralizes cross-cutting error logic — mapping domain exceptions to HTTP statuses, building a uniform error body, and logging — so every endpoint behaves consistently. You can scope an advice to specific packages or annotated controllers." },
      { kind: "code", lines: ["@RestControllerAdvice(basePackages = \"com.shop.api\")   // scoped advice", "public class ApiExceptionHandler {", "    @ExceptionHandler({IllegalArgumentException.class, IllegalStateException.class})", "    public ResponseEntity<ApiError> badRequest(RuntimeException ex) {", "        return ResponseEntity.badRequest()", "            .body(new ApiError(400, ex.getMessage()));", "    }", "}"] },
      { kind: "note", text: "If multiple @ExceptionHandler methods could match, Spring picks the most specific exception type. A handler for a superclass acts as a fallback for its subclasses." },
    ],
    learn: "spring boot global exception handling ControllerAdvice",
  },
  {
    q: "What is the purpose of the @ExceptionHandler annotation?",
    slug: "exception-handler-annotation",
    answer: [
      { kind: "text", text: "@ExceptionHandler marks a method that handles one or more exception types thrown by controller handler methods. When a matching exception occurs, Spring invokes this method instead of letting the error bubble up, and uses its return value (or @ResponseStatus) to build the response." },
      { kind: "text", text: "Interview angle: placed in a controller it's local to that controller; placed in a @ControllerAdvice it becomes global. The handler method can accept the exception, the request, etc., and return a ResponseEntity, a view, or a body." },
      { kind: "code", lines: ["@RestController", "public class OrderController {", "    @ExceptionHandler(OrderNotFoundException.class)", "    public ResponseEntity<String> handle(OrderNotFoundException ex) {", "        return ResponseEntity.status(HttpStatus.NOT_FOUND)", "                             .body(ex.getMessage());", "    }", "}"] },
      { kind: "note", text: "One method can handle several types via @ExceptionHandler({A.class, B.class}); the method parameter should be a common supertype of them." },
    ],
    learn: "spring ExceptionHandler annotation usage",
  },
  {
    q: "What is the purpose of the @ControllerAdvice annotation?",
    slug: "controller-advice-annotation",
    answer: [
      { kind: "text", text: "@ControllerAdvice is a specialization of @Component that defines classes assisting ALL (or a subset of) controllers. It centralizes three cross-cutting concerns: global @ExceptionHandler methods, @ModelAttribute methods that add common model data, and @InitBinder methods for data-binding customization." },
      { kind: "text", text: "Interview angle: it's the backbone of global exception handling in Spring MVC. Because handler methods here typically return views/models, for a pure REST/JSON API you'd usually use @RestControllerAdvice (which adds @ResponseBody) instead." },
      { kind: "code", lines: ["@ControllerAdvice", "public class GlobalAdvice {", "    @ExceptionHandler(Exception.class)", "    public String handle(Exception ex, Model model) {", "        model.addAttribute(\"error\", ex.getMessage());", "        return \"error-page\";        // resolves to a view", "    }", "    @ModelAttribute(\"appName\")", "    public String appName() { return \"Shop\"; }   // shared model data", "}"] },
      { kind: "note", text: "You can target it with attributes like annotations = RestController.class or basePackages to limit which controllers it advises." },
    ],
    learn: "spring ControllerAdvice annotation purpose",
  },
  {
    q: "What is the purpose of the @RestControllerAdvice annotation?",
    slug: "rest-controller-advice-annotation",
    answer: [
      { kind: "text", text: "@RestControllerAdvice is a convenience annotation that combines @ControllerAdvice and @ResponseBody. It's the go-to for REST APIs: its @ExceptionHandler methods return objects that are automatically serialized to JSON (via Jackson) as the response body, rather than being resolved as view names." },
      { kind: "text", text: "Interview angle: use @RestControllerAdvice for JSON/REST APIs and plain @ControllerAdvice for MVC apps that return views. Functionally the only difference is the implicit @ResponseBody on every handler method." },
      { kind: "code", lines: ["@RestControllerAdvice", "public class RestExceptionHandler {", "    @ExceptionHandler(ResourceNotFoundException.class)", "    @ResponseStatus(HttpStatus.NOT_FOUND)", "    public ApiError handle(ResourceNotFoundException ex) {", "        return new ApiError(404, ex.getMessage());  // -> JSON body", "    }", "}", "", "record ApiError(int status, String message) {}"] },
      { kind: "note", text: "Because @ResponseBody is implied, you don't need it (nor a ResponseEntity) just to return a JSON body — though ResponseEntity is still handy when you want fine control over headers/status." },
    ],
    learn: "spring RestControllerAdvice annotation rest api",
  },
  {
    q: "What is the purpose of the @Transactional annotation in Spring Boot?",
    slug: "transactional-annotation",
    answer: [
      { kind: "text", text: "@Transactional declares that a method (or all methods of a class) should run inside a database transaction. Spring wraps the call with a proxy that begins a transaction before the method, commits on normal return, and rolls back on a runtime exception — giving you atomicity without manual commit/rollback." },
      { kind: "text", text: "Interview angle: by default it rolls back only on unchecked (RuntimeException/Error) exceptions, not checked ones; override with rollbackFor. It also exposes propagation and isolation settings to control transaction boundaries and concurrency." },
      { kind: "code", lines: ["@Service", "public class TransferService {", "    @Transactional(rollbackFor = Exception.class)", "    public void transfer(Long from, Long to, BigDecimal amt) {", "        accountRepo.debit(from, amt);", "        accountRepo.credit(to, amt);   // if this throws, debit rolls back too", "    }", "}"] },
      { kind: "note", text: "Big gotcha: it works via a proxy, so self-invocation (a method in the same class calling another @Transactional method) bypasses the proxy and the transaction is NOT applied. Also don't put it on private methods." },
    ],
    learn: "spring Transactional annotation propagation rollback",
  },
  {
    q: "Explain the concept of transaction management.",
    slug: "transaction-management",
    answer: [
      { kind: "text", text: "Transaction management ensures a group of operations obey ACID properties (Atomicity, Consistency, Isolation, Durability) — they all succeed or all roll back. Spring offers declarative management via @Transactional (the common way) and programmatic management via TransactionTemplate or PlatformTransactionManager for fine-grained control." },
      { kind: "text", text: "Interview angle: Spring Boot auto-configures the right PlatformTransactionManager (JpaTransactionManager for JPA, DataSourceTransactionManager for JDBC). Key knobs are propagation (how nested transactions behave, e.g. REQUIRED vs REQUIRES_NEW) and isolation (what concurrent transactions can see)." },
      { kind: "example", text: "A bank transfer: debit account A and credit account B must be one atomic unit. If the credit fails after the debit, transaction management rolls the debit back so money is never lost." },
      { kind: "code", lines: ["@Transactional(propagation = Propagation.REQUIRES_NEW,", "               isolation = Isolation.READ_COMMITTED,", "               timeout = 5)", "public void auditLog(String msg) { ... }  // always its own transaction"] },
    ],
    learn: "spring transaction management ACID propagation isolation",
  },
  {
    q: "Explain the role of Spring Security in Spring Boot applications.",
    slug: "spring-security-role",
    answer: [
      { kind: "text", text: "Spring Security is the framework for authentication (who are you) and authorization (what are you allowed to do). Adding spring-boot-starter-security auto-secures every endpoint with HTTP Basic and a generated login, then you customize it by defining a SecurityFilterChain bean. It works as a chain of servlet filters in front of your app." },
      { kind: "text", text: "Interview angle: in Spring Boot 3 / Security 6 you configure it with a component-based, lambda DSL SecurityFilterChain bean (WebSecurityConfigurerAdapter is removed). Method-level rules use @PreAuthorize. It also provides protection against CSRF, session fixation, and common attacks by default." },
      { kind: "code", lines: ["@Configuration", "@EnableWebSecurity", "public class SecurityConfig {", "    @Bean", "    SecurityFilterChain chain(HttpSecurity http) throws Exception {", "        http", "          .authorizeHttpRequests(a -> a", "              .requestMatchers(\"/public/**\").permitAll()", "              .anyRequest().authenticated())", "          .httpBasic(Customizer.withDefaults());", "        return http.build();", "    }", "}"] },
      { kind: "note", text: "Enable annotation-based authorization with @EnableMethodSecurity, then guard methods: @PreAuthorize(\"hasRole('ADMIN')\")." },
    ],
    learn: "spring security spring boot 3 SecurityFilterChain",
  },
  {
    q: "How does Spring Boot handle security vulnerabilities like XSS and CSRF?",
    slug: "xss-csrf-protection",
    answer: [
      { kind: "text", text: "CSRF: Spring Security enables CSRF protection by default for browser-based (stateful) apps, requiring a synchronizer token on state-changing requests (POST/PUT/DELETE). XSS: Spring helps by escaping output in Thymeleaf/JSP by default and setting security headers (X-Content-Type-Options, X-Frame-Options, and a configurable Content-Security-Policy)." },
      { kind: "text", text: "Interview angle: for stateless REST APIs authenticated by tokens (JWT/OAuth), CSRF is commonly disabled because there's no cookie-based session to exploit. Escaping user input on output and validating input are the primary XSS defenses — the framework escapes, but you must not bypass it." },
      { kind: "code", lines: ["http", "  // stateless JWT API: CSRF not needed", "  .csrf(csrf -> csrf.disable())", "  .headers(h -> h", "      .contentSecurityPolicy(csp -> csp", "          .policyDirectives(\"default-src 'self'\"))", "      .frameOptions(fo -> fo.deny()));   // clickjacking defense"] },
      { kind: "note", text: "Only disable CSRF when the app is truly stateless (no session cookies). If you use cookie-based auth, keep CSRF on — disabling it is a real vulnerability." },
    ],
    learn: "spring security csrf xss protection configuration",
  },
  {
    q: "How can you enable Cross-Origin Resource Sharing (CORS)?",
    slug: "enable-cors",
    answer: [
      { kind: "text", text: "CORS controls which foreign origins (scheme+host+port) a browser may let call your API. In Spring Boot you enable it per-handler with @CrossOrigin, or globally via a WebMvcConfigurer's addCorsMappings, or through a CorsConfigurationSource bean when Spring Security is present." },
      { kind: "text", text: "Interview angle: CORS is enforced by the browser, not the server — the server just sends Access-Control-Allow-* headers telling the browser what's permitted. Preflight OPTIONS requests are handled automatically once CORS is configured." },
      { kind: "code", lines: ["@Configuration", "public class CorsConfig implements WebMvcConfigurer {", "    @Override", "    public void addCorsMappings(CorsRegistry registry) {", "        registry.addMapping(\"/api/**\")", "            .allowedOrigins(\"https://app.example.com\")", "            .allowedMethods(\"GET\", \"POST\", \"PUT\", \"DELETE\")", "            .allowCredentials(true);", "    }", "}"] },
      { kind: "note", text: "With Spring Security you must also let CORS through the filter chain: http.cors(Customizer.withDefaults()). And allowedOrigins(\"*\") cannot be combined with allowCredentials(true) — use allowedOriginPatterns instead." },
    ],
    learn: "enable cors spring boot rest api configuration",
  },
  {
    q: "What is the purpose of the @CrossOrigin annotation?",
    slug: "cross-origin-annotation",
    answer: [
      { kind: "text", text: "@CrossOrigin enables CORS at the fine-grained level — on a single controller class or an individual handler method. It lets you declare exactly which origins, methods and headers are allowed for those endpoints, without a global configuration." },
      { kind: "text", text: "Interview angle: it's convenient for quick or endpoint-specific rules. For consistent, app-wide policy prefer a global WebMvcConfigurer/CorsConfigurationSource; use @CrossOrigin when one endpoint needs different treatment." },
      { kind: "code", lines: ["@RestController", "@CrossOrigin(origins = \"https://app.example.com\")   // whole controller", "public class ProductController {", "    @GetMapping(\"/products\")", "    public List<Product> all() { ... }", "", "    @CrossOrigin(origins = \"https://admin.example.com\")  // override per method", "    @DeleteMapping(\"/products/{id}\")", "    public void delete(@PathVariable Long id) { ... }", "}"] },
      { kind: "note", text: "Default @CrossOrigin with no attributes allows all origins and methods — be explicit in production rather than leaving it wide open." },
    ],
    learn: "spring CrossOrigin annotation controller method",
  },
  {
    q: "How do you enable CORS globally?",
    slug: "enable-cors-globally",
    answer: [
      { kind: "text", text: "Register a single global policy by implementing WebMvcConfigurer and overriding addCorsMappings, so one place governs CORS for all matching paths. When Spring Security is on the classpath, additionally expose a CorsConfigurationSource bean and enable http.cors() so the security filter chain honors it." },
      { kind: "text", text: "Interview angle: global config keeps the policy consistent and auditable in one file, versus @CrossOrigin scattered across controllers. Remember Security intercepts requests first, so without http.cors() the WebMvc mapping alone may not take effect." },
      { kind: "code", lines: ["@Bean", "CorsConfigurationSource corsSource() {", "    CorsConfiguration cfg = new CorsConfiguration();", "    cfg.setAllowedOrigins(List.of(\"https://app.example.com\"));", "    cfg.setAllowedMethods(List.of(\"GET\", \"POST\", \"PUT\", \"DELETE\"));", "    cfg.setAllowCredentials(true);", "    var src = new UrlBasedCorsConfigurationSource();", "    src.registerCorsConfiguration(\"/**\", cfg);", "    return src;", "}", "// in SecurityFilterChain: http.cors(Customizer.withDefaults());"] },
      { kind: "note", text: "Pick one global mechanism. Mixing WebMvcConfigurer mappings and a CorsConfigurationSource bean can be confusing — with Spring Security, the CorsConfigurationSource bean is the authoritative one." },
    ],
    learn: "spring boot global cors configuration WebMvcConfigurer",
  },
  {
    q: "How can you enable logging in a Spring Boot application?",
    slug: "enable-logging",
    answer: [
      { kind: "text", text: "Logging is built in — Spring Boot uses SLF4J as the facade and Logback as the default implementation (pulled in by spring-boot-starter-logging inside every starter). You just obtain a logger and use it; no extra dependency needed." },
      { kind: "text", text: "Interview angle: configure levels and output through application.properties (logging.level.*, logging.file.name), or drop a logback-spring.xml for full control. You can switch to Log4j2 by excluding the default starter and adding spring-boot-starter-log4j2." },
      { kind: "code", lines: ["public class OrderService {", "    private static final Logger log =", "        LoggerFactory.getLogger(OrderService.class);   // SLF4J", "    public void place(Order o) {", "        log.info(\"Placing order {}\", o.getId());   // parameterized, no concat", "        log.debug(\"details: {}\", o);", "    }", "}", "", "// application.properties", "// logging.level.com.shop=DEBUG", "// logging.file.name=app.log"] },
      { kind: "note", text: "Use parameterized messages (log.info(\"x={}\", x)) not string concatenation — the argument is only rendered if that level is enabled, so it's cheaper and cleaner." },
    ],
    learn: "spring boot logging slf4j logback configuration",
  },
  {
    q: "How can you enable request logging?",
    slug: "enable-request-logging",
    answer: [
      { kind: "text", text: "For built-in HTTP request/response logging, register a CommonsRequestLoggingFilter (or AbstractRequestLoggingFilter) bean, which logs the URI, query string, headers and payload. Alternatively, write a custom OncePerRequestFilter or an interceptor for tailored logging." },
      { kind: "text", text: "Interview angle: CommonsRequestLoggingFilter is the quick, framework-provided option — enable payload logging and raise the org.springframework.web.filter logger to DEBUG to see the output. For richer, structured logs many teams write a filter and add correlation/trace IDs (e.g. via MDC)." },
      { kind: "code", lines: ["@Bean", "public CommonsRequestLoggingFilter requestLogging() {", "    var f = new CommonsRequestLoggingFilter();", "    f.setIncludeQueryString(true);", "    f.setIncludePayload(true);", "    f.setMaxPayloadLength(2048);", "    f.setIncludeHeaders(false);", "    return f;", "}", "// logging.level.org.springframework.web.filter.CommonsRequestLoggingFilter=DEBUG"] },
      { kind: "note", text: "Be careful logging payloads and headers in production — they may contain passwords, tokens or PII. Mask sensitive fields and mind the log volume." },
    ],
    learn: "spring boot request logging CommonsRequestLoggingFilter",
  },
  {
    q: "How can you enable Swagger / OpenAPI documentation?",
    slug: "enable-swagger-openapi",
    answer: [
      { kind: "text", text: "For Spring Boot 3 add the springdoc-openapi-starter-webmvc-ui dependency. It scans your @RestController endpoints, generates an OpenAPI 3 spec at /v3/api-docs, and serves an interactive Swagger UI at /swagger-ui.html automatically — no configuration required to get started." },
      { kind: "text", text: "Interview angle: the older springfox library doesn't support Spring Boot 3 (jakarta namespace), so springdoc-openapi is the current standard. You enrich the docs with annotations like @Operation, @Schema and a custom OpenAPI bean for title/version/security." },
      { kind: "code", lines: ["<!-- Spring Boot 3 -->", "<dependency>", "    <groupId>org.springdoc</groupId>", "    <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>", "</dependency>", "", "@RestController", "public class ProductController {", "    @Operation(summary = \"Get a product by id\")", "    @GetMapping(\"/products/{id}\")", "    public Product get(@PathVariable Long id) { ... }", "}"] },
      { kind: "note", text: "Common trap in interviews: naming springfox for a Boot 3 app. It's incompatible; use springdoc-openapi (starter artifact springdoc-openapi-starter-webmvc-ui)." },
    ],
    learn: "springdoc openapi swagger ui spring boot 3 setup",
  },
  {
    q: "How does Spring Boot support testing of applications?",
    slug: "testing-support",
    answer: [
      { kind: "text", text: "spring-boot-starter-test bundles JUnit 5, Mockito, AssertJ, Hamcrest and Spring Test. The key annotations are @SpringBootTest for full integration tests (loads the whole context) and slice-test annotations like @WebMvcTest, @DataJpaTest and @JsonTest that load only the relevant layer for fast, focused tests." },
      { kind: "text", text: "Interview angle: use slices to keep tests fast — @WebMvcTest spins up only the web layer with MockMvc and lets you @MockBean the service; @DataJpaTest configures an in-memory DB and rolls back each test. Reserve @SpringBootTest for true end-to-end coverage." },
      { kind: "code", lines: ["@WebMvcTest(ProductController.class)", "class ProductControllerTest {", "    @Autowired MockMvc mvc;", "    @MockBean ProductService service;    // mock the collaborator", "", "    @Test", "    void returnsProduct() throws Exception {", "        when(service.find(1L)).thenReturn(new Product(1L, \"Pen\"));", "        mvc.perform(get(\"/products/1\"))", "           .andExpect(status().isOk())", "           .andExpect(jsonPath(\"$.name\").value(\"Pen\"));", "    }", "}"] },
      { kind: "note", text: "In Spring Boot 3.4+ @MockBean/@SpyBean are being replaced by @MockitoBean/@MockitoSpyBean — worth mentioning to show you're current." },
    ],
    learn: "spring boot testing SpringBootTest WebMvcTest slices",
  },
];
